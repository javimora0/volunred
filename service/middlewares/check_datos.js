const model = require('../models')

const existe_email = async(email = '') => {
    // TODO: Checkear en la base de datos si existe ese email
}
const existe_username = async(username = '') => {
    // TODO: Checkear en la base de datos si existe ese nombre de usuario
}
const check_dni_nie = async (dni_nie) => {

}
const existe_dni_nie = async (dni_nie) => {

}

const existe_telefono = async (telefono) => {

}

module.exports = {
    existe_email,
    existe_username,
    check_dni_nie,
    existe_dni_nie,
    existe_telefono
}